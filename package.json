{
  "name": "vulcan-web-opcua-client",
  "version": "1.0.0",
  "description": "Vulcan WebSocket OPC UA 浏览器客户端（docs/08-websocket-transport.md §5：@wsopcua/wsopcua）",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=esm --outfile=dist/app.js",
    "test": "node test/ws_web_client_test.mjs"
  },
  "dependencies": {
    "@wsopcua/wsopcua": "^0.12.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "ws": "^8.18.0"
  }
}
